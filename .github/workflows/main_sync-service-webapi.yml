# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - sync-service-webapi

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to container registry
        uses: docker/login-action@v2
        with:
          registry: cloudedgesyncacr.azurecr.io/
          username: ${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_3AFC11C6073741F5B40D1947D8F32F77 }}
          password: ${{ secrets.AZUREAPPSERVICE_CONTAINERPASSWORD_F6E28D5F50B04F2FA859E3A7306F9308 }}

      - name: Build and push container image to registry
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: cloudedgesyncacr.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_3AFC11C6073741F5B40D1947D8F32F77 }}/sync-service-webapi:${{ github.sha }}
          file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    needs: build
    
    steps:
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_96693305F753428D927F9AEBF43D05FD }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_1B6D200F11364837981A4D1210FD60B8 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_8E258870AC704C78970BD788361802C2 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'sync-service-webapi'
          slot-name: 'Production'
          images: 'cloudedgesyncacr.azurecr.io/${{ secrets.AZUREAPPSERVICE_CONTAINERUSERNAME_3AFC11C6073741F5B40D1947D8F32F77 }}/sync-service-webapi:${{ github.sha }}'
          